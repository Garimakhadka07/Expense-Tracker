name: an_expense_tracker
description: A Flutter expense tracker app with Firebase
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.15.1
  firebase_auth: ^5.6.2
  firebase_database: ^11.3.10


  intl: ^0.18.1
  fl_chart: ^0.68.0
  provider: ^6.1.5
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true